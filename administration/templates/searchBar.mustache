<section class="hero is-primary">
    <div class="hero-body">
        <div class="columns is-centered">
            <div class="column is-narrow">
                <form method="post">
                    Search a {{eventType}} Event by Name
                    <input class="input" type="text" value="{{value}}" id="search" name="{{eventType}}" placeholder="Enter the name of {{eventType}}">
                    <input class="button" type="submit" value="Search">
                </form>
            </div>
        </div>
    </div>
</section>

